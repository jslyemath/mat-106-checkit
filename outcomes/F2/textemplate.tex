\setvseed{\VAR{seed}}
\skillheader{F2}

%% Every picture here comes from the .tikz the generator wrote, which is the
%% same file CheckIt rasterizes for the website. One source, two surfaces.
%%
%% This template used to draw its own pgfplots axis for the number lines while
%% the generator drew a different one for the web -- two drawings of one
%% picture, disagreeing on arrowheads, tick length, line weight and width.
%%
%% The problem/answer choice is made twice below, at two different times, and
%% the difference matters.
%%
%% The Jinja conditionals run when this file is written, and decide which files
%% are named at all. printit copies figures by scanning the written text, so
%% naming one that does not exist fails the build even inside a LaTeX branch
%% that would never run. Area-model questions have no problem picture -- the
%% task is to draw one -- so theirs must not be named.
%%
%% The anstoggle conditionals run when the PDF is built, choosing between the
%% student copy and the key. Every file they name exists by then.
%%
%% (Written in prose deliberately: a Jinja tag spelled out in a comment here is
%% still a Jinja tag, and this file failed to render once for that reason.)

\begin{enumerate}
    \item \VAR{p1_text}$\VAR{p1_math}$.

    \vspace{20pt}
    \BLOCK{if p1_type == 'line'}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p1_ans_model.tikz}%
    }{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p1_prob_model.tikz}%
    }
    \BLOCK{else}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p1_ans_model.tikz}%
    }{}
    \BLOCK{endif}

    \vfill

    \item \VAR{p2_text}$\VAR{p2_math}$.

    \vspace{20pt}
    \BLOCK{if p2_type == 'line'}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p2_ans_model.tikz}%
    }{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p2_prob_model.tikz}%
    }
    \BLOCK{else}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p2_ans_model.tikz}%
    }{}
    \BLOCK{endif}

    \vfill

    \item \VAR{p3_text_math}.

    \vspace{20pt}
    \BLOCK{if p3_type == 'line'}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p3_ans_model.tikz}%
    }{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p3_prob_model.tikz}%
    }
    \BLOCK{else}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2/generated/\VAR{'%04d'|format(seed)}/p3_ans_model.tikz}%
    }{}
    \BLOCK{endif}

    \vfill
\end{enumerate}
