\setvseed{\VAR{seed}}
\skillheader{F2-E}

    Suppose a student of yours is asked to complete the following fraction modeling task.

    \begin{statement}
        \VAR{p1_text}$\VAR{p1_math}$.
    \end{statement}
    
    Construct the desired model. Then \textbf{explain} each step of its construction process. How is the denominator represented in the model? How is the numerator represented? How does each inform the construction process?

    %% The picture comes from the .tikz the generator wrote -- the same file
    %% CheckIt rasterizes for the website, so both show the same thing. See
    %% F2's textemplate for why the problem/answer choice is made twice, once
    %% in Jinja and once in LaTeX.
    \vspace{20pt}
    \BLOCK{if p1_type == 'line'}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2-E/generated/\VAR{'%04d'|format(seed)}/p1_ans_model.tikz}%
    }{%
        \input{assets/F2-E/generated/\VAR{'%04d'|format(seed)}/p1_prob_model.tikz}%
    }
    \BLOCK{else}
    \ifthenelse{\boolean{anstoggle}}{%
        \input{assets/F2-E/generated/\VAR{'%04d'|format(seed)}/p1_ans_model.tikz}%
    }{}
    \BLOCK{endif}

    \VAR{p1_soln_text}
