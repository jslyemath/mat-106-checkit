\setvseed{\VAR{seed}}
\skillheader{D1}

\begin{enumerate}
    \item Consider the number $\VAR{pv_dec_string}$. Identify and name each of the place values used in this number. Write these names using both a numerical label (e.g.\ ``10s'') and an English-word label (e.g.\ ``tens'').
Numeric labels must use fractions for values smaller than one.


    \vspace{20pt}
    \begin{ansenv}
        \VAR{pv_ans_text}
    \end{ansenv}
    \vfill

    \item Using \VAR{units_block_choice_a} to represent the units, draw a base-ten block representation of the number \VAR{blocks_dec_a}.

    \vspace{20pt}
    \begin{ansenv}
        \VAR{blocks_ans_text_a}
    \end{ansenv}
    \vfill

    \item Using \VAR{units_block_choice_b} to represent the units, draw a base-ten block representation of the number \VAR{blocks_dec_b}.

    \vspace{20pt}
    \begin{ansenv}
        \VAR{blocks_ans_text_b}
    \end{ansenv}
    \vfill
\end{enumerate}
